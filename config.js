'use strict';

module.exports = {
    local: {
        env: 'local',
        api: 'http://path-to-local'
    },
    production: {
        env: 'production',
        api: 'http://path-to-production'
    }
};